{
  "name": "parse-patch",
  "version": "0.1.4",
  "description": "A utility for parsing git patch files generated by `git format-patch`.",
  "repository": "dexaai/parse-patch",
  "author": "Riley Tomasek <riley@dexa.ai>",
  "license": "MIT",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run scripts/cleanDist.ts && tsc",
    "release": "bun run build && np",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "np": "^10.1.0",
    "typescript": "^5.0.0"
  }
}
